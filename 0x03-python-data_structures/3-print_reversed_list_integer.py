#!/usr/bin/python3
def print_reversed_list_integer(my_list=[]):
    if True:
        lis_len = len(my_list)
        my_list.reverse()
        for list in range(lis_len):
            print("{:d}".format(my_list[list]))
